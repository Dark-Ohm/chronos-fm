# HANDOFF — контекст для новой сессии Архитектора (Chronos-FM)

**Обновлено: 2026-08-07 (чекпоинт #5), HEAD `957a546`.** Цель сессии:
«перекрасить Chronos-FM в Chronos + перенести дизайн-паттерны +
доработать до состояния замены Thunar / Dolphin». Читать этот блок
первым, §0-§5 ниже — история до 2026-07-18, остаётся верной.

## Чекпоинт #5, 2026-08-07 — пять тикетов закрыты живьём, T021 со
## второго захода, три ошибки брифа

**Закрыто и подтверждено живым прогоном** (не тестами — живьём, под
Hyprland, ydotool + grim):

| Тикет | Чем подтверждено |
|---|---|
| T017 Git follow | Заход в репо → `main`, 16 modified + 14 untracked, **Refresh не нажимался**; выход → пустое состояние, тоже без Refresh |
| T018 accent | Семь свотчей = blue/green/purple/orange/red/teal/pink; при `accent="purple"` обведён фиолетовый |
| T019 иконки | `could not find asset` — **0**; иконка носителя и eject видны; путь с эллипсисом |
| T020 recursive size | Диалог `4.9 KB (4 files, 2 dirs)` против `du -sb ~/.ssh` = 5046 байт, 4 файла, 2 директории |
| T021 S3 листинг | После connect **без навигации** 2 бакета; заход в бакет — `data/`, `notes/`, `readme.txt` **31 B**, байт в байт засеянное |

`cargo test --workspace` → **293 passed, 0 failed** (pages 87 после T021).

**T021 потребовал двух заходов, и виноват был мой бриф.** Первый заход
(`32cb1ac`) заменил `loaded = false` на
`pane.downgrade().update_in(...)` — компилируется, молча не работает:
`WeakEntity::update_in` резолвит окно через `current_window_by_entity`,
которую заполняют только `defer_in`/`observe_in`/`subscribe_in`, а
панель создана через `cx.new`. В логе была ровно одна строка,
`s3.rs:165: entity has no current window`.

Мой бриф назвал, *что* сделать, но не предупредил о стене: прямой
`Entity::update_in` требует `C: VisualContext`, которым `Context<S3Page>`
не является, — **не компилируется**. Исполнитель пошёл искать обход и
нашёл тот, что тихо ломается. Переписанный бриф начинается с разбора
провала, и второй заход (`ab61c4a`) сработал: окно не теряется,
`wire_pane` делает обычный `pane.update`, тест дёргает production-метод
**через S3Page**, а не панель напрямую.

**Три моих ошибки за сессию — записать и не повторять:**

1. **`git add` по каталогу утащил чужую работу.** В `6352d8f` я
   застейджил `docs/orchestration/tasks/active/` целиком и закоммитил
   созданный исполнителем каталог `hold/`, похоронив в нём тикеты T010 и
   T011. Обнаружено через день, откачено в `fc5d2ec`. **Стейджить
   пофайлово.**
2. **Ложное утверждение по неудачному грепу.** Заявил, что `.log_err()`
   в проекте никуда не пишет. Неверно: `tracing_subscriber::fmt().try_init()`
   ставит мост `log → tracing` (`tracing-log` в лок-файле), сообщения
   выходят на уровне **WARN** с таргетом `chronos_fm_core::telemetry`.
   Я грепал по `ERROR`. Именно эта строка потом и доказала корень T021.
3. **Непроверяемые критерии приёмки.** В бриф T021 записал «запросы в
   логе RustFS» и «строки модуля `chronos_fm_services::s3`». Ни того,
   ни другого система не производит: RustFS не ведёт access-лог (5 строк
   за запуск, все стартовые), модуль не пишет debug. Рабочий критерий —
   **сами данные**: имена бакетов и размер объекта совпадают с
   засеянными.

**Находка исполнителя, ценнее самого фикса:** `use super::*` в
тест-модуле затягивает `gpui::test` (proc-macro) через `use gpui::*`
родителя, и сгенерированный `#[gpui::test]` резолвит свой внутренний
`#[test]` обратно в макрос → `recursion limit`. Импортировать явно, а не
поднимать `#![recursion_limit]`. Вероятно, это же объясняет deferred
render-тест из T010.

**Инструмент:** для живой проверки S3 прижился **RustFS**
(Apache-2.0, не AGPL как MinIO), образ `docker.io/rustfs/rustfs:latest`,
ключи `rustfsadmin`/`rustfsadmin`, API `:9000`. Грабля: под rootless
podman bind-маунт хостовых каталогов даёт `PermissionDenied` на `/data` —
только именованные тома. Наполнение — `seed_s3.py` (boto3 есть в
системе; `aws`/`mc`/`rclone` — нет).

**Грабли живого прогона под Hyprland:** ydotool **удваивает**
абсолютные координаты (слать половину); окно перетасовывается тайлингом,
за сессию геометрия менялась пять раз — читать `hyprctl clients` перед
**каждой** серией кликов; `hyprctl dispatch setfloating address:…` в этой
Lua-сборке не парсится; раскладка должна быть английской, иначе
`ydotool type` печатает кириллицу.

**Прибрано:** все отчёты в `report/` получили врезку с вердиктом — до
этого судьбу отчёта нельзя было отличить от неотсуженного, а T021 вообще
заявлял «выполнено» для опровергнутой работы. Статус-блоки тикетов
T010/T011/T014/T015 переписаны — они несли статусы, устаревшие на
несколько сессий («не начинать код, нет spec и плана» при сданных
милестоунах).

**Состояние:** `active/` 5, `report/` 6, `done/` 17, `report-log/` 20.
Осталось: T010 (Milestone B/C, разблокирован, долг clippy закрыт
`daa6807`), T011 (разблокирован закрытием T021, нужен полный живой
прогон S3), T014 (зонтичный, ждёт цифр T022), T015 (один пункт — высота
карточки Places, нужно решение архитектора), T022 (готов к раздаче,
часть D изолирована).

## Чекпоинт #4, 2026-08-06 — приёмка всех 10 отчётов, живые смоки,
## T017/T018/T019

**Приёмка велась как судейство:** отчёт — набор утверждений, а не
доказательство. Ни одно «проверено» не принято на слово; всё
перепрогнано (`cargo test --workspace --no-fail-fast`, `git log -S`,
`cargo tree -i`, живой прогон релизного бинаря под Hyprland).

**Вердикты по 10 отчётам в `report/`:**

| Отчёт | Вердикт |
|---|---|
| T009 settings | VERIFIED (+ живой смок) |
| T010 git | VERIFIED WITH CAVEATS — сервис рабочий, follow-режим мёртв → T017 |
| T011 s3 Milestone A | **REFUTED** |
| T011 s3 Milestones B | VERIFIED WITH CAVEATS |
| T012 extensions | VERIFIED (+ живой смок) |
| T013 test coverage | **REFUTED** |
| T014 recon (первый) | VERIFIED как recon |
| T014 perf-замер (второй) | VERIFIED — `/tmp/perf_*.data` на диске совпали |
| T015 sidebar | VERIFIED WITH CAVEATS — 3 из 6 пунктов не закрыты |
| T016 search | VERIFIED WITH CAVEATS (+ живой смок) |

**Главное открытие — ложная атрибуция блокера сборки.** T013, T014-recon
и T016 независимо записали ashpd-конфликт как «предсуществующий, в
форке». `cargo tree -i ashpd -e features` даёт цепочку
`chronos-fm-core → oo7 (default) → oo7/tokio → ashpd/tokio` против
`gpui_linux → ashpd/async-io`, а `git log -S oo7 --
crates/chronos-fm-core/Cargo.toml` — **единственный** коммит `07bce8b`
(T011 Tasks 5+6). Блокер внесли мы. Ошибка размножилась ссылками
отчётов друг на друга. Исправлено `0564c6e`
(`default-features = false, features = ["async-std", "native_crypto"]`).

**Почему это не ловилось:** `cargo check -p chronos-fm-pages` зелёный,
ломается только `--workspace` (feature unification). Отсюда правило:
**заявление «собирается» без указания области — не доказательство.**

**Четыре падения тестов, скрытых за блокером** (после снятия —
`cargo test --workspace --no-fail-fast` → **277 passed, 0 failed**,
коммит `05a6e5a`):
1. `committed_schema_is_up_to_date` — `docs/config.schema.json` без `s3`.
2. `from_toml_str_output_snapshot` — снапшот без `s3`; в `07bce8b` вместо
   `insta accept` закоммичен pending `.snap.new`. T011 назвал оба падения
   «pre-existing, не связаны с S3» — их диффы **ровно** S3-блок.
3-4. `mode_to_rwx_known_modes` и `format_permissions_reads_real_mode` —
   боевой баг: маски строились из битов владельца (`0o400/0o200/0o100`)
   и сдвигались влево ещё раз, поэтому итерации владельца и группы
   маскировали `0o40000`/`0o4000` и никогда не совпадали. **Properties
   показывал `------rwx` для каждого файла.** Тесты, которые это ловят,
   T013 написал, но по собственному признанию не компилировал.

**Живые смоки** (release-бинарь, Hyprland, ydotool + grim; скриншоты
`smoke-01`…`smoke-24` в скратчпаде сессии):
- T009: клик Dark → `diff` конфига дал **ровно одну строку** `5c5`,
  inline-комментарии и все 50 строк целы; тема применилась без
  перезапуска (watcher), активная кнопка следует за файлом.
- T016: `Failed to initialize search service` — **0 вхождений**,
  индекс `2750 documents`.
- T012: три секции + пустое состояние с TOML-сниппетом.
- T011: `NoProfiles`-карточка рисуется (кнопки «Open Settings →» из
  отчёта Milestone A на ней нет — потеряна при переписывании в B.3/B.4).
- T010: после Refresh — ветка `main`, 6 modified + 6 untracked,
  совпало с `git status`.
- T013: после фикса Properties даёт `rwxr-xr-x (0755)` против
  `ls -l` → `-rwxr-xr-x`.

**Заведены тикеты (`ec345dd`):**
- **T017** (P1) — Git follow-режим не следует за Explorer. Заход в репо
  панель игнорирует, выход — тоже; живёт только по кнопке Refresh.
  Воспроизведено в обе стороны, Pin выключен. Гипотеза (не корень):
  poll-луп тикает только по сигналу `refresh_tx` от `GitWatcher`, а
  навигация Explorer в этот канал ничего не шлёт.
- **T018** (P2) — `settings.rs:100` зовёт `gpui::rgba()` с 6-значными
  константами `ACCENT_PALETTE`. `rgba` ждёт `0xRRGGBBAA` → красный канал
  теряется, альфа берётся из синего. `purple 0x8b5cf6` рисуется зелёным,
  `orange 0xe8930c` с альфой `0x0c`. Наблюдаемые цвета совпали со
  сдвигом побайтово по всем семи. Внесено в `f0ab77e`.
- **T019** (P2) — нет `icons/hard-drive.svg` и `icons/arrow-up.svg`;
  две строки ERROR **на каждый кадр**, кнопка eject невидима. Внесено в
  `9403f57` (T015), где стояла галочка «иконки не упираются в край ✅»,
  полученная чтением кода при непроведённом живом прогоне.

**Ловушка сборки (стоила одного захода):** в корневом `Cargo.toml` есть
`default-members` **без GUI-крейтов**, поэтому голый
`cargo build --release` бинарь не собирает — молча остаётся старый
артефакт. Нужен `cargo build --release -p chronos-fm`. Я прогнал часть
смоков против бинаря от 18:43 и получил «фикс не работает»; поймано
сверкой `stat` исходника и бинаря.

**Мелочи, не заведённые тикетами:** окно не выставляет `app_id`
(`hyprctl clients` даёт пустые `class`/`title`); Properties печатает
время в UTC при локальном +03; листинг не перечитывается при повторном
заходе в тот же каталог и не реагирует на изменения ФС.

**Не тронуто по решению пользователя:** `skills/` (6 МБ дубля
`~/.claude/skills`, копия `chronos-shell` разошлась — `md5` repo
`0b2b9402` против home `0f2e7300`; 17 скиллов есть только в репо и они
про ChronOS/gpui-форк, не про этот проект), `.workbuddy/`, `docs/fable/`,
`.claude/settings.json`, `script/dev/patch_theme_calls.py`.

**Состояние тикетов:** ничего не перемещено в `done/`. Кандидаты —
T009, T012, T016 (код + живые смоки подтверждены). T010 держать в
`active/` до закрытия T017. T011 и T013 — код исправлен, но тексты
отчётов содержат опровергнутые утверждения, нужна эррата. T015 — часть
ушла в T019. T014 — фикс в работе у пользователя.

**Урок сессии:** дважды строил выводы на устаревших данных — листинг
`report/` (за время приёмки вырос с 8 до 10 файлов, два отчёта я едва не
пропустил) и mtime бинаря. Оба раза ловилось перепроверкой. Правило:
**состояние каталогов и артефактов перечитывать перед выводом, а не
один раз в начале.**

## Чекпоинт #3, 2026-08-06 — приёмки T007/T008/T006, корень 20fps,
## тикеты T014/T015/T016

**Принято с оговоркой и закоммичено:** T007 (Open With / service menu)
+ T008 (Devices клики mount/unmount + фикс `eject` на Drive-путь) —
`8ba8b81`; b1–b4 (context-menu chain) + T006 (Batch Rename) — `8077e6b`;
fork path-deps на `../Source` — `9790480` (коммит пользователя).
Приёмка велась **независимой проверкой**, а не чтением отчётов:
прогон тестов + спот-чек заявленных фиксов в коде. Это дало результат —
см. «расхождения» ниже.

**Корень жалобы «20 fps вместо 144» найден и устранён.**
`~/.local/bin/chronos-fm` был симлинком на `target/debug/` (981 МБ), а
секции `[profile.dev]` в `Cargo.toml` **не было вовсе** → все зависимости
(gpui, wgpu/blade, шейпинг шрифтов, layout) собирались на `opt-level = 0`.
Фикс `58e0928`: зависимости `opt-level = 3` + `debug = false`, свой код
`opt-level = 1`; debug-бинарь 981 МБ → 184 МБ. Симлинк перевешен на
`target/release/chronos-fm` (44 МБ, сборка 6м06с). Оценка пользователя
после: **«не летает, но гораздо лучше»** — значит под debug-множителем
сидит вторая, настоящая проблема → **T014**.

**Исключено живьём (не проверять повторно):** софт-рендера нет, лог даёт
`Selected GPU adapter: "NVIDIA GeForce RTX 3070" (Vulkan)`, дискретка.
Остались vsync/композитор и код рендера.

**Инцидент «wofi запускает прошлый бинарь»:** симлинк, PATH и `.desktop`
были верны — висел процесс `742118` (debug, с 12:17), державший
эксклюзивный лок на `state.redb` (`redb error: Database already open`).
Убит, лок освободился.

**Заведены тикеты:**
- **T014** — рендер-перф до 144fps, намеренно тикет *расследования*:
  замер обязателен ДО правок; описана ловушка `strip = "debuginfo"` в
  release (профайлер получит пустой стек); отмечено, что список уже
  виртуализирован. Recon-отчёт сдан (`db1ddd9`).
- **T015** — редизайн сайдбара. Верхний блок `Home/Favorites/Recent/
  Trash` (`sidebar.rs:28-31`, `sidebar_item` — `42-61`) **вообще без
  `on_click`**: четыре мёртвые кнопки; дубль `Home` с живой карточкой
  ниже; три несогласованные карточки подряд; `unmount`/`eject` сырым
  текстом впритык к краю; `get_shortcuts()` делает 4 `stat` на каждый
  кадр. **Нужен брейншторм с пользователем** — судьба мёртвых пунктов.
- **T016** — поиск умирал целиком: watcher вешается на весь `$HOME`
  рекурсивно (`search/watcher.rs:39`), спотыкается о podman-volume
  `drwx------ uid 100999`, `?` роняет весь `SearchService` в `Err` —
  и это видно только строкой в логе. Исправлено агентом (`459ea2b`).

**Очередь на момент чекпоинта:** T009 (Settings) и T010 (Git)
**верифицированы** мной (workspace 242 passed / 0 failed, `config::patch::`
6/6, git 14/14 поимённо, все фиксы по ревью подтверждены в коде),
вердикт accepted-with-concerns, но **намеренно не перемещены** в
`report-log/`/`done/` — решение пользователя принимать пачкой вместе с
T011/T012. T011 (S3) идёт по Milestone B.1–B.3. **T012 заблокирован
целиком** — `chronos-fm-plugin-host` закомментирован в
`[workspace] members`, P4 не начат.

**Найденные расхождения отчётов (закрыть follow-up'ом):**
1. T010 пишет «clippy 0 замечаний», но проверял только
   `-p chronos-fm-services`, обойдя собственный новый UI-файл Task 3 —
   `pages/src/git.rs` (443 стр): 4× неиспользуемый
   `let this = cx.weak_entity();` (87, 187, 211, 239), 2× `unwrap()`
   (300, 337 — под guard'ом, паники не будет, но workspace-линт
   `unwrap_used = "warn"` нарушен), missing docs.
2. T008 писал «eject не вызывается из UI», а в коде
   (`sidebar.rs:279-311`) он есть и кликабелен — код опережал отчёт.

**Урок сессии:** в дереве параллельно работают другие агенты. Поймал
гонку — в первый коммит T006-тикета попала стерая версия файла
(агент писал между моим `Edit` и `git mv`); обнаружено сверкой
`git show HEAD:<файл>` с диском, поправлено `--amend`. Сверять диффы
перед стейджингом, не доверять `git status` слепо, чужие
незакоммиченные правки не коммитить.

## Чекпоинт #2, 2026-08-06 — T003 accepted-with-concerns, T004/T005 в
## параллельной работе (НЕ мной), T009 (Settings) dispatch-ready

**T003 (Devices панель, udisks2/zbus) — принят с оговоркой
(`done/`).** Весь пайплайн (парсер→backend→store→mount/unmount-логика→
hotplug-подписка→рендер) рабочий, живьём подтверждён (build/test +
реальный запуск, реальные udisks2-разделы `sdb` — «VTOYEFI»/«Ventoy» —
живьём увидены в sidebar). Гэп: **клики не подключены** — это
недоработка САМОГО implementation plan (Task 6 расписал только
рендер, не wiring кликов — моя ошибка при написании плана, не
исполнителя). Плюс дремлющий баг: `eject()` бьёт не по тому D-Bus
объекту (Block-путь вместо Drive-пути), не проявляется, т.к. в v1
eject не вызывается. Открыт **T008** на оба пункта, ОСТОРОЖНО — не
кликать «mount» по реальным разделам без физической тестовой флешки.

**Инцидент сессии — я по ошибке заспавнил своих субагентов.** При
переходе к исполнению T003 плана взял `subagent-driven-development`
skill по дефолту (пользователь сказал «1» = subagent-driven execution
mode, не разрешение спавнить агентов) и реально задиспатчил Task 1
через Agent tool — прямое нарушение правила из корневого CLAUDE.md
«НЕ спавнишь своих субагентов, оркестрируешь локальных агентов
Архитектора». Пользователь поймал, спросил «почему пошли субагенты? я
не понял». Убил фонового агента до коммита, откатил его незакоммиченные
правки (`git checkout` + `rm -rf` нового модуля), вернулся к T-тикет
конвенции — написал T003 как обычный тикет, пользователь его раздал.
**Урок:** «1»/«2» в контексте subagent-driven vs inline execution — это
ВСЕГДА про формат ВНЕШНЕГО execution (через T-тикет минионам), не про
разрешение мне самому спавнить Agent-тулом, если roleplay этой сессии
— Lead Architect, не исполнитель.

**Обнаружено при этом чекпоинте (не мной сделано) — T004+T005 ушли в
параллельную работу.** Тикеты T004 (архивы-как-папка) и T005
(Properties/Permissions диалог) были явно помечены «ТРЕКИНГ-ТИКЕТ, не
раздавать как есть — сначала брейншторм со мной». Кто-то (вероятно
GPT 5.6 sol, параллельная сессия) тем не менее сам сделал полный цикл
brainstorm→spec (`8e764e4`, `8a9a10f`, включая РАСШИРЕНИЕ скоупа — .7z/
.rar добавлены сверх изначального zip/tar/tar.gz/tar.zst)→implementation
(`9c0587b`, `61d3db9`, `33f97e5`)→report (`df5ffd5`, лежит в
`docs/orchestration/tasks/report/T004-archive-browsing-report.md` и
`T005-properties-dialog-report.md`). Процесс соблюдён на уровне файлов —
тикеты остались в `active/`, отчёты в `report/`, НЕ самоутверждены — но
это ПАРАЛЛЕЛЬНАЯ работа мимо согласованного пути. **Отчёты НЕ приняты,
ждут моей живой приёмки** (build/test/grep/live smoke) следующей
сессией — не доверять на слово, особенно расширенному скоупу
(.7z/.rar — RAR лицензионно НЕсвободный формат, `unrar` крейт может
тянуть непонятную лицензию, сверить `deny.toml` при приёмке).

**T009 (Settings tab live) — spec+plan написаны, dispatch-ready,
активный тикет, ещё НЕ раздан на момент чекпоинта.** Живое
редактирование `config.toml` (theme/ui/explorer) через
`toml_edit::DocumentMut` (точечная правка, комментарии/форматирование
СОХРАНЯЮТСЯ — жёсткое требование из брейншторма с пользователем).
Черновые секции (keybindings/plugins/indexing/search/launcher) —
показаны задизейблены, не редактируются.

**T010–T012 — трекинг, НЕ раздавать без брейншторма:** Git-таб
(P2, нужен git2 vs shell-обёртка решение), S3-таб (P3, клиент+
credentials решение), Extensions-таб (P4, блокирован P4-стартом
plugin-host целиком, даже brainstorm рано).

**T006/T007 — тоже трекинг, ждут: T006 (batch rename) жёстко зависит
от b1 (не в done на момент чекпоинта — сверить), T007 (open with)
независим, можно брейнштормить в любой момент.**

## Чекпоинт 2026-08-05 — reskin-трек запущен, T001+T002 приняты

Заведена T-нумерованная оркестрация в этом репо (по образцу ChronOS,
но локальный счётчик — НЕ путать с T-ID в ChronOS, разные проекты):
`docs/orchestration/tasks/{active,report,report-log,rejected,done}`.
Design spec: `docs/superpowers/specs/2026-08-05-theme-reskin-design.md`.

**Параллельно существует независимый функциональный трек b1–b4**
(`docs/agents/active/` — clipboard/rename, file-ops, context-menu
row+grid, план `docs/superpowers/plans/2026-07-21-explorer-context
-menu.md`) — старая b-схема этого репо, НЕ T-нумерована, не трогать
файлы `row.rs`/`list.rs`/`grid.rs`/`listing.rs` пока они в работе.
Не раздана на момент чекпоинта (`docs/agents/report/` пуст).

**T001 (theme reskin infra) — ПРИНЯТ, `done/`.** Единый источник цвета:
`gpui_component::Theme` (у gpui-component уже был готовый light/dark
toggle-движок, `Theme::change()` был наполовину подключён в `root.rs`,
просто красил дефолтную палитру) + новый `crates/chronos-fm/assets/
themes/chronos.theme.json` (140 ключей/режим, сгенерирован
`script/dev/gen_chronos_theme.py` из Base16-сида — тот же сид, что
`../ChronOS/crates/ui/src/theme/schemes.rs::DEFAULT_BASE16` (Mocha dark)
и `light_scheme()` ("Light C"). Мост `crates/chronos-fm-ui/src/theme.rs`
— 20 бывших `pub const` → `pub fn name(cx: &App) -> Hsla`, читают
`cx.theme()`. Живая приёмка архитектором (не только компиляция):
grim+пипетка обоих режимов, `bg` dark `#1e1e2e` и light `#dde0f2` —
точное совпадение с seed. Найдена и исправлена ошибка: dark-акцент
случайно был Mocha-mauve вместо `#007acc` (правило «акцент не красится
темой» нарушалось) — поймано архитектором до приёмки, не пользователем.

**T002 (elevated_card + section_header) — ПРИНЯТ, `done/`.** Новый
`crates/chronos-fm-ui/src/patterns.rs`, порт ChronOS T231-паттерна (без
`elevation_apply_light_chrome` — гпуи-компонентный `.shadow_md()`
эквивалент). Применён в `sidebar.rs` (секция Folders) и `settings.rs`.
Живой кадр подтвердил `sidebar.rs` (dark); `settings.rs` принят по
код-ревью — попытка синтетического клика (`ydotool`) на живом столе
промахнулась мимо gear-иконки в чужое окно (на столе шла не связанная
с сессией активность), решил не долбить вслепую повторными кликами.

**Empty-state паттерн — НЕ портирован, сознательно.** Источник в ChronOS
— T252 (`docs/orchestration/tasks/active/T252-*.md` в ChronOS), сам ещё
не закрыт на момент этого чекпоинта (ждёт отчёта от GPT 5.6 sol). Заводить
T003 в Chronos-FM только после того как T252 закроется в ChronOS.

**Практическая находка обеих приёмок:** `cargo build ... | tail -N` в
отчётах глушит реальный exit-код cargo (bash без `pipefail` — экзит-код
принадлежит `tail`, не билду). Проверять `cargo build ... 2>&1; echo
EXIT=$?` или без пайпа вовсе — T001-исполнитель наступил, T002-исполнитель
уже учёл это в своём отчёте.

**Очередь:** T003 (empty-state, после ChronOS T252) — не заведён. b1–b4
можно раздавать независимо в любой момент (файловые зоны не пересекаются
с reskin-треком). Хвосты §2 старого HANDOFF (app_id, doc-sync,
search-permission) — не в очереди, ждут запроса.

## 0. Как этот репо вообще выглядит (важно, было недоразумение)

Это НЕ тот же самый рабочий каталог, что был до 2026-07-18 — старый
каталог содержал документы, наплетённые локальной 3B-моделью
(галлюцинации: корневые `ARCHITECTURE.md`/`MEMORY.md` оказались
дословными копиями РАННИХ доков ChronOS-шелла, плюс выдуманный
release-notes на несуществующий релиз `v0.1.0-cachy1`). По запросу
пользователя старый каталог снесён (перемещён в
`../Chronos-FM.bak-3b-<таймстамп>`, не удалён физически) и репо
переклонировано начисто:

```
gh repo view Dark-Ohm/chronos-fm  # публичный, не приватный (пользователь
                                   # ошибочно помнил его приватным)
git clone git@github.com:Dark-Ohm/chronos-fm.git Chronos-FM
```

**Канон документов теперь — только то, что реально в git-истории этого
клона**: `docs/architecture.md` (актуальная архитектура, японский),
`docs/ROADMAP.md` (P1–P6 фазы), `CHANGELOG.md`, `README.md`. Корневых
`ARCHITECTURE.md`/`MEMORY.md`/`SESSION_REPORT.md`/`SOUL.md` в чистом
клоне НЕТ — если они появятся снова (например, кто-то скопирует их из
бэкапа), это НЕ канон, а рецидив 3B-мусора — не доверять, сверять с
`docs/`. `.rules` в корне — это конфиг агента (Factory Droid, generic
Rust/GPUI гайдлайны), не архитектура проекта; `AGENTS.md`/`CLAUDE.md` —
однострочные указатели на него (`.rules`), это нормально, не баг.

Git identity в этом репо — локальный (не global) `dark-ohm` /
`dohm.labs@proton.me`, тот же что в ChronOS. БЕЗ AI-трейлеров в коммитах
(та же политика, что в ChronOS — глобальное правило пользователя, не
специфичное для одного репо).

## 1. Что такое Chronos-FM

"Launcher × Explorer" — Raycast-style лаунчер + keyboard-driven файловый
эксплорер в одном приложении, форк `nohrs` (см. `dce6124` — "renamed
from nohrs"), портируется под Linux/Hyprland лично пользователем.
MIT, `github.com/Dark-Ohm/chronos-fm` (публичный).

Воркспейс (7 активных крейтов, `Cargo.toml`):
`chronos-fm` (bin) / `chronos-fm-core` / `chronos-fm-models` /
`chronos-fm-services` / `chronos-fm-store` / `chronos-fm-ui` /
`chronos-fm-pages` (самый жирный, ~6700 строк — эксплорер).
`chronos-fm-launcher` (P3) и `chronos-fm-plugin-host` (P4) — ЕЩЁ НЕ
СОЗДАНЫ, только закомментированы в `[workspace] members`.

Фазы (`docs/ROADMAP.md`): P1 Foundation (сейчас, `0.0.x`) → P2 Explorer
Essentials (`0.1.0`) → **P3 Launcher & Search (`0.2.0`, лаунчера ещё нет
физически)** → **P4 Plugin Host (`0.3.0`) — РЕШЕНО 2026-07-18: Luau
(`chronos_luau`), НЕ WASM.** WASM Component Model/wit-bindgen/wasmtime
(ADR 0005) отменено целиком — см. [ADR 0009](docs/adr/0009-luau-plugin-host-supersedes-wasm.md),
ADR 0005 помечен Superseded. Rust/TS/Python плагин-темплейты отменены.
Не реализовано ни в одном варианте — P4 не начат, менять было дёшево.
Открытый хвост: расшарить `chronos_luau` из ChronOS path-депом (по
аналогии с GPUI-тулкитом) или завести отдельную копию — не решено, не
начинать `chronos-fm-plugin-host` без этого решения. → P5 Ecosystem
→ P6 Stabilization.

## 2. Единый тулкит с ChronOS (сделано 2026-07-18)

Решение брейнсторм-сессии: один GPUI-форк на оба проекта — наш
`../Source` (gpui-ce chronos edition), НЕ upstream zed/crates.io.
Причина: у обоих проектов свой лаунчер по плану — по-другому
несопоставимо и глупо плодить два форка одного тулкита.

**Выполнено GROK'ом (задание в `GROK.md`, отчёт в
`report-log/grok-report-1.md`), ПРИНЯТО мной лично** (`8c2a7f4`,
живой смок + `cargo tree` + тесты перепроверены, не поверил отчёту на
слово):
- `gpui`/`gpui_platform`/`gpui-component` теперь path-депы на
  `../Source/gpui`, `../Source/gpui_platform`,
  `../Source/gpui-component/crates/ui` (были crates.io: `gpui="0.2"`,
  `gpui-component="0.5"`).
- `[patch."https://github.com/zed-industries/zed"]` в корневом
  `Cargo.toml` — `gpui-component`'овский вложенный воркспейс всё ещё
  объявляет zed-siblings как git-депы; патч унифицирует граф на один
  path-`gpui` (иначе Cargo резолвит ДВА разных `gpui` и падает).
  Подтверждено `cargo tree --workspace -i gpui` — один узел.
- Bootstrap: `Application::new()` (upstream-паттерн) не существует в
  нашем форке публично → заменено на `gpui_platform::application()`
  (`crates/chronos-fm/src/app.rs`) — тот же паттерн, что ChronOS
  `main.rs`.
- Дрейф API — маленький, 4 места: `flex_grow()` →
  `flex_grow_1()`/`flex_grow(f32)` (`explorer/view.rs`,
  `pane_group.rs`), `FocusHandle::focus(window)` →
  `focus(window, cx)` (`pane_group.rs`).
- `Source/` — 0 правок (было условие задания, read-only).
- Живой смок (мой, не только Grok'а): `cargo build --workspace` +
  `cargo test --workspace` (171/171) чисто; бинарь реально открывает
  окно на этой Hyprland-сессии, `gpui_wgpu` реально выбирает
  "NVIDIA GeForce RTX 3070 (Vulkan)" — подтверждает, что это НАШ
  форк-рендерер работает, а не заглушка.

**Хвосты (не блокеры, не трогать без запроса пользователя):**
- Окно `chronos-fm` имеет пустые `class`/`title` в `hyprctl clients -j`
  (нужен `app_id` — мешает window rules/поиску по имени).
- `docs/architecture.md`/README не обновлены под новую path-deps
  конвенцию (Grok сам это отметил как долг).
- Search-сервис падает с `Permission denied` на
  `~/.local/share/containers/storage/volumes/hindsight_pg_data/_data`
  (индексатор пытается сканировать podman-volume, куда нет доступа) —
  **не связано с миграцией тулкита**, pre-existing, некритично (app не
  падает, просто ищет без индекса). Отдельная задача, если пользователь
  захочет её закрыть.

## 3. Лаунчер — РЕШЕНО 2026-07-18: разные по назначению, не сливать

Изначальный план сессии — сравнить лаунчер ChronOS (`SUPER+L`,
минималистичный, XDG toplevel, `crates/app/src/launcher/` в ChronOS) с
лаунчером Chronos-FM. Оказалось: в Chronos-FM лаунчера физически нет
(P3, только `[launcher]` в `settings.toml`: `hotkey` дефолт
`Cmd+Shift+Space` — macOS-наследие, `position_remember`). Сравнивать
было нечего.

**Решение пользователя:** лаунчеры остаются РАЗНЫМИ по задаче, не
сливаются в один UX/хоткей.
- ChronOS `SUPER+L` — быстрый системный app-launcher, часть шелла,
  работает всегда независимо от того, запущен ли Chronos-FM.
- Chronos-FM (P3, будущий) — "глубокий поиск" по файлам/командам/
  git-статусу, привязанный к данным эксплорера (SQLite+Tantivy индекс) —
  другая задача, даже если стилистически похож (Raycast-style).

Не пересматривать это без явного нового запроса пользователя — компромисс
(единый UX против меньшей связанности) был явно взвешен и решён в пользу
разделения при текущем масштабе проекта.

## 4. Линты и code style, принесённые В ChronOS ИЗ этого проекта

При сравнении кода (см. `docs/architecture.md` §5 здесь) выяснилось,
что дисциплина линтов в Chronos-FM строже: `unsafe_code = deny`,
`clippy::unwrap_used`/`expect_used = warn`, запрет глушить ошибки
`let _ =`. Эти три вещи перенесены В ChronOS (`ChronOS/CLAUDE.md` §"Код
— правила", commit `93f917c` в ChronOS) — НЕ в этот репо, они тут уже
были. Обратного переноса (мод.rs-запрет, cargo-deny tokio-бан,
missing_docs, ADR-файлы) в ChronOS НЕ произошло — рассмотрено и
осознанно отклонено, см. ChronOS `CLAUDE.md` для причин каждого пункта.

## 5. Минион-конвенция для этого репо

Та же схема, что в ChronOS: задание → `<ИМЯ>.md` в корне этого репо
(не ChronOS!) → пользователь скармливает агенту → отчёт в
`<имя>-report.md` → Архитектор принимает лично (грепы/сборка/живой
смок), после приёмки переносит отчёт в `report-log/<имя>-report-N.md`
git-мувом (не просто `rm`, иначе может "воскреснуть" при неосторожном
git-мусоре других сессий/агентов — см. инцидент в ChronOS MEMORY.md).
Пока в этом репо был только Grok (задание №1, принято).

## Очередь

1. Хвосты §2 (app_id, README/architecture.md doc-sync, search
   permission) — не назначены, ждут запроса пользователя.
2. Лаунчер (P3) — решение принято (§3, разделены), реализация не
   начата, не в очереди пока пользователь не попросит.
3. Plugin-host (P4) — решение принято (Luau, §2, ADR 0009), но есть
   ОТКРЫТЫЙ хвост перед стартом реализации: расшарить `chronos_luau` из
   ChronOS path-депом или завести отдельную копию в Chronos-FM — не
   решено, спросить пользователя перед началом `chronos-fm-plugin-host`.
4. `docs/ROADMAP.md`/`docs/plugin-*.md` — P4-секции всё ещё описывают
   WASM построчно (см. баннер вверху ROADMAP.md) — переписать при
   реальном старте P4, не заранее.
