# T012 — Extensions-таб: витрина плагинов вместо заглушки

**Приоритет:** P4 — жёстко зависит от P4 роадмапа (`chronos_luau`
plugin-host, ADR 0009), физически не начат в проекте. Показывать
витрину нечего, пока хост не существует.
**Статус:** ТРЕКИНГ-ТИКЕТ, не начинать код, даже brainstorm рано.

## Блокер

`chronos-fm-plugin-host` крейт закомментирован в `[workspace] members`
(`Cargo.toml`) — P4 не начат физически. HANDOFF.md §1: «Открытый хвост:
расшарить `chronos_luau` из ChronOS path-депом или завести отдельную
копию — не решено, не начинать `chronos-fm-plugin-host` без этого
решения». Это не про UI-заглушку, это архитектурное решение уровня
проекта.

## Что делать

Ничего пока. Держать тикет как маркер очереди. Разбирать только после
явного запроса пользователя начать P4 — тогда сначала решение про
`chronos_luau` (расшарить vs копия), потом plugin-host крейт, потом
UI-виджина сверху.

## Зависимости

Блокирован P4-стартом целиком, не только этой вкладкой.
