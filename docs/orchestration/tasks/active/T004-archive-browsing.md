# T004 — архивы как виртуальная папка (browse без распаковки)

**Приоритет:** P2 — второй по критичности гэп до Dolphin-паритета
(частый повседневный кейс: открыть .zip не распаковывая на диск).
**Статус:** ТРЕКИНГ-ТИКЕТ, не начинать код. Нет ни design spec, ни
implementation plan — backlog-пункт #1 из `docs/superpowers/specs/2026
-08-06-removable-media-mount.md` (низ файла), только приоритет одной
строкой, не проработано.

## Что нужно ПЕРЕД кодом

1. `brainstorming` skill: решить — свой виртуальный FS-слой (открыть
   `.zip`/`.tar.*` как псевдо-директорию в текущем эксплорере, читать
   через `zip`/`tar` крейты без записи на диск) vs интеграция с уже
   существующим (gvfs-подобного в проекте нет — вероятно свой слой).
   Определить: какие форматы v1 (zip точно, tar.gz/tar.zst — решить),
   read-only или можно редактировать содержимое архива, как это
   стыкуется с существующей `chronos-fm-services::fs::listing`
   абстракцией (архивная «папка» должна давать тот же интерфейс
   листинга, что реальная директория, иначе UI придётся ветвить).
2. Design spec → `docs/superpowers/specs/`.
3. Implementation plan → `docs/superpowers/plans/`.
4. Только после этого — T-тикет на код (следующий свободный номер на
   момент готовности плана, не резервировать заранее).

## Зависимости

Не зависит от T003 (независимая зона). Может параллелиться с T003 в
работе, если оба на этапе brainstorm/design — но код обеих задач трогает
`crates/chronos-fm-services/src/fs/` в разной степени, сверить зоны
файлов при написании plan для этого тикета, если T003 к тому моменту
уже что-то там занял.

## Не раздавать как есть

Этот файл — не инструкция для исполнителя-кодера. Если отдать в
текущем виде — исполнитель либо откажется (нет brief), либо начнёт
гадать архитектуру сам. Сначала брейншторм с архитектором.
